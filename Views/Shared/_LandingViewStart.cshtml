

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="" />
    <meta name="author" content="" />
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @Styles.Render("~/Content/styles/styles.css")
    @section Scripts {
        @Scripts.Render("~/bundles/jquery")
        @Scripts.Render("~/bundles/jqueryval")
    }
</head>
<body>

        <div>
            <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
                <div class="container">
                    <a class="navbar-brand" href="#">Warehouse®</a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                            aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarResponsive">
                        <ul class="navbar-nav ml-auto navbar-edit">
                            <li class="nav-item">
                                @Html.ActionLink("Home", "home_supplier", "Landing", new { area = "" }, new { @class = "nav-link" })
                            </li>
                            <li class="nav-item">
                                @Html.ActionLink("Products", "home_product", "Landing", new { area = "" }, new { @class = "nav-link" })
                            </li>
                            <li class="nav-item" id="supplierControlNav" runat="server">
                                @if (Session["Username"] != null)
                                {
                                @Html.ActionLink("Admin", "supplierControl", "Supplier", new { area = "" }, new { @class = "nav-link" })
                                }
                            </li>
                            <li class="nav-item" id="loginNav" runat="server">
                                @Html.ActionLink("Login", "Login", "Account", new { area = "" }, new { @class = "nav-link" })
                            </li>
                            <li class="nav-item">
                                <span class="nav-link text-white" id="usernameNav" runat="server"></span>
                            </li>
                            <li class="nav-item" id="logoutNav" runat="server">
                                @if (Session["Username"] != null)
                                {
                                    @Html.ActionLink("Logout", "Logout", "Account", new { area = "" }, new { @class = "nav-link" })
                                }
                            </li>
                        </ul>
                    </div>
                </div>
            </nav>

            <!-- Page content-->
            @RenderBody()
            @*<footer>
                @using (Html.BeginForm("mailAdd", "mail", FormMethod.Post))
                {
                    <div class="container-fluid footer-ground">
                        <div class="footer-extra">
                            <h1 style="color: #fbfd00">
                                Contact Us
                            </h1>
                            <p class="lead">
                                Feel free to contact us if you have any problem
                            </p>
                        </div>
                        <div class="footer-form">
                            <div class="mb-3">
                                <label for="nameForm" class="form-label">Name</label>
                                <input ID="nameForm" name="senderName" class="form-control" placeholder="Enter your name">
                            </div>
                            <div class="mb-3">
                                <label for="emailForm" class="form-label">Email Address</label>
                                <input ID="emailForm" name="emailName" class="form-control" placeholder="Enter your email address">
                            </div>
                            <div class="mb-3">
                                <label for="messageForm" class="form-label">Your Message</label>
                                <textarea ID="messageForm" name="comment" class="form-control" Rows="3" placeholder="Comment"></textarea>
                            </div>
                            <div class="col-12">
                                <button type="submit" href="@Html.ActionLink("Logout", "Logout", "Account", new { area = "" }, new { @class = "nav-link" })" class="btn btn-primary button-yellow-design"> GO CONTACT US</button>
                            </div>
                        </div>
                        <hr />
                        <div class="footer-extra">
                            © Start, 2022. All rights reserved.
                        </div>
                    </div>
                }
            </footer>*@
            <!-- Bootstrap core JS-->
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
            <!-- Core theme JS-->
            <script src="App_Themes\Project_Theme\js\scripts.js"></script>
        </div>
    
</body>
</html>
